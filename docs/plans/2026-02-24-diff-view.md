# Diff View Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add a `[d]` keybinding to the ccn dashboard that opens a live, scrollable git diff TUI for the selected session's working directory.

**Architecture:** A new `DiffView` Ink/React component is added as a third view mode (`'diff'`) in `Dashboard.tsx`, following the same pattern as `TaskDetailView`. `DiffView` fetches the git diff synchronously via `execFileSync` on mount and file-selection change, renders colored diff lines in a scrollable window, and handles all its own keyboard input.

**Tech Stack:** Ink v5, React 18, Node.js `child_process.execFileSync`, `node:path`

> **No unit tests for this feature:** `DiffView` is a pure UI component with synchronous git side effects — the existing test suite covers `handler`, `file-store`, `focus`, and `send-text` utilities, not Ink components. Verification is via `npm run typecheck`, `npm run lint`, and manual smoke test.

---

### Task 1: Create `DiffView` component — shell + no-changes state

**Files:**
- Create: `src/components/DiffView.tsx`

**Step 1: Create the file with imports and a no-changes skeleton**

```tsx
import { execFileSync } from 'node:child_process';
import { basename, resolve } from 'node:path';
import { Box, Text, useInput, useStdout } from 'ink';
import type React from 'react';
import { useEffect, useState } from 'react';
import type { Session } from '../types/index.js';

interface DiffFile {
  path: string;
  added: number;
  removed: number;
}

interface DiffViewProps {
  session: Session;
  onExit: () => void;
}

function runGit(args: string[], cwd: string): string {
  try {
    return execFileSync('git', args, {
      cwd,
      encoding: 'utf-8',
      stdio: ['pipe', 'pipe', 'pipe'],
    });
  } catch {
    return '';
  }
}

function getChangedFiles(cwd: string): DiffFile[] {
  const parseNumstat = (text: string): Map<string, DiffFile> => {
    const map = new Map<string, DiffFile>();
    for (const line of text.trim().split('\n')) {
      if (!line) continue;
      const parts = line.split('\t');
      if (parts.length >= 3) {
        map.set(parts[2], {
          path: parts[2],
          added: Number.parseInt(parts[0]) || 0,
          removed: Number.parseInt(parts[1]) || 0,
        });
      }
    }
    return map;
  };

  const unstaged = parseNumstat(runGit(['diff', '--numstat'], cwd));
  const staged = parseNumstat(runGit(['diff', '--cached', '--numstat'], cwd));

  const merged = new Map<string, DiffFile>(unstaged);
  for (const [p, f] of staged) {
    if (!merged.has(p)) {
      merged.set(p, f);
    } else {
      const existing = merged.get(p) as DiffFile;
      merged.set(p, {
        ...existing,
        added: existing.added + f.added,
        removed: existing.removed + f.removed,
      });
    }
  }
  return Array.from(merged.values());
}

function getDiffLines(cwd: string, filePath: string): string[] {
  const unstaged = runGit(['diff', '--', filePath], cwd);
  const staged = runGit(['diff', '--cached', '--', filePath], cwd);
  const combined = (unstaged || staged).trim();
  return combined ? combined.split('\n') : [];
}

function DiffLine({ line }: { line: string }): React.ReactElement {
  if (line.startsWith('+') && !line.startsWith('+++')) {
    return <Text color="green">{line}</Text>;
  }
  if (line.startsWith('-') && !line.startsWith('---')) {
    return <Text color="red">{line}</Text>;
  }
  if (line.startsWith('@@')) {
    return <Text color="cyan">{line}</Text>;
  }
  if (
    line.startsWith('diff ') ||
    line.startsWith('index ') ||
    line.startsWith('---') ||
    line.startsWith('+++')
  ) {
    return <Text dimColor>{line}</Text>;
  }
  return <Text>{line}</Text>;
}

export function DiffView({ session, onExit }: DiffViewProps): React.ReactElement {
  const { stdout } = useStdout();
  const [files, setFiles] = useState<DiffFile[]>([]);
  const [selectedFileIndex, setSelectedFileIndex] = useState(0);
  const [diffLines, setDiffLines] = useState<string[]>([]);
  const [scrollOffset, setScrollOffset] = useState(0);

  const terminalHeight = stdout?.rows ?? 24;
  // Chrome overhead: file-strip box (3) + header row (1) + diff borders (2) + footer (1) = 7
  const visibleLines = Math.max(5, terminalHeight - 7);

  useEffect(() => {
    const changed = getChangedFiles(session.cwd);
    setFiles(changed);
    setSelectedFileIndex(0);
  }, [session.cwd]);

  useEffect(() => {
    if (files.length > 0 && selectedFileIndex < files.length) {
      setDiffLines(getDiffLines(session.cwd, files[selectedFileIndex].path));
      setScrollOffset(0);
    } else {
      setDiffLines([]);
    }
  }, [session.cwd, files, selectedFileIndex]);

  useInput((input, key) => {
    if (input === 'q' || key.escape || input === 'd') {
      onExit();
      return;
    }
    if (input === 'k') {
      setSelectedFileIndex((prev) => Math.max(0, prev - 1));
      return;
    }
    if (input === 'j') {
      setSelectedFileIndex((prev) => Math.min(files.length - 1, prev + 1));
      return;
    }
    if (input === 'w' || key.upArrow) {
      setScrollOffset((prev) => Math.max(0, prev - 5));
      return;
    }
    if (input === 's' || key.downArrow) {
      const maxOffset = Math.max(0, diffLines.length - visibleLines);
      setScrollOffset((prev) => Math.min(maxOffset, prev + 5));
      return;
    }
    if (input === 'e' && files[selectedFileIndex]) {
      try {
        execFileSync('code', [resolve(session.cwd, files[selectedFileIndex].path)], {
          stdio: 'ignore',
        });
      } catch {
        // VS Code not in PATH — silently ignore
      }
    }
  });

  const dir = session.cwd.replace(/^\/Users\/[^/]+/, '~');

  if (files.length === 0) {
    return (
      <Box flexDirection="column">
        <Box flexDirection="column" borderStyle="round" borderColor="cyan" paddingX={1}>
          <Box>
            <Text bold color="cyan">
              Git Diff
            </Text>
            <Text dimColor> — {dir}</Text>
          </Box>
          <Box marginTop={1}>
            <Text dimColor>No changes detected in this directory.</Text>
          </Box>
        </Box>
        <Box marginTop={1} justifyContent="center">
          <Text dimColor>[d/q/Esc]Back</Text>
        </Box>
      </Box>
    );
  }

  const currentFile = files[selectedFileIndex];
  const visibleDiffLines = diffLines.slice(scrollOffset, scrollOffset + visibleLines);
  const totalAdded = files.reduce((sum, f) => sum + f.added, 0);
  const totalRemoved = files.reduce((sum, f) => sum + f.removed, 0);
  const maxOffset = Math.max(0, diffLines.length - visibleLines);
  const scrollPercent = maxOffset > 0 ? Math.round((scrollOffset / maxOffset) * 100) : 100;

  return (
    <Box flexDirection="column">
      {/* File strip */}
      <Box borderStyle="round" borderColor="gray" paddingX={1} gap={1}>
        {files.map((f, i) => (
          <Text key={f.path} bold={i === selectedFileIndex} color={i === selectedFileIndex ? 'white' : 'gray'}>
            {i === selectedFileIndex ? `[${basename(f.path)}]` : basename(f.path)}
          </Text>
        ))}
      </Box>

      {/* File header */}
      <Box paddingX={1} gap={2}>
        <Text bold color="white">
          {basename(currentFile.path)}
        </Text>
        <Text color="green">+{currentFile.added}</Text>
        <Text color="red">-{currentFile.removed}</Text>
        <Text dimColor>{currentFile.path}</Text>
      </Box>

      {/* Diff content */}
      <Box flexDirection="column" borderStyle="round" borderColor="gray" paddingX={1}>
        {visibleDiffLines.length === 0 ? (
          <Text dimColor>No diff content</Text>
        ) : (
          visibleDiffLines.map((line, i) => (
            // biome-ignore lint/suspicious/noArrayIndexKey: position-stable diff rendering
            <DiffLine key={scrollOffset + i} line={line} />
          ))
        )}
      </Box>

      {/* Footer */}
      <Box justifyContent="space-between" paddingX={1}>
        <Box gap={1}>
          <Text dimColor>{files.length} files</Text>
          <Text color="green">+{totalAdded}</Text>
          <Text color="red">-{totalRemoved}</Text>
          {maxOffset > 0 && <Text dimColor>{scrollPercent}%</Text>}
        </Box>
        <Box gap={1}>
          <Text dimColor>[j/k]Files</Text>
          <Text dimColor>[w/s]Scroll</Text>
          <Text dimColor>[e]VSCode</Text>
          <Text dimColor>[d/q]Back</Text>
        </Box>
      </Box>
    </Box>
  );
}
```

**Step 2: Verify the file saved correctly**

```bash
head -5 src/components/DiffView.tsx
```
Expected: first 5 lines show the imports.

**Step 3: Run typecheck**

```bash
npm run typecheck
```
Expected: no errors related to `DiffView.tsx`.

---

### Task 2: Wire `DiffView` into `Dashboard.tsx`

**Files:**
- Modify: `src/components/Dashboard.tsx`

**Step 1: Add `'diff'` to `ViewMode` and import `DiffView`**

At line 15 in `Dashboard.tsx`, change:
```tsx
type ViewMode = 'list' | 'tasks';
```
to:
```tsx
type ViewMode = 'list' | 'tasks' | 'diff';
```

Add the import at the top (after the existing component imports):
```tsx
import { DiffView } from './DiffView.js';
```

**Step 2: Add early return for `'diff'` mode in `useInput`**

In the `useInput` callback, after the existing `if (viewMode === 'tasks')` block, add:
```tsx
if (viewMode === 'diff') {
  return; // DiffView handles its own input
}
```

**Step 3: Add `[d]` handler in the list-mode input block**

After the `if (input === 's') { openTaskView(); ... }` block, add:
```tsx
if (input === 'd') {
  if (sessions[selectedIndex]) {
    setViewMode('diff');
  }
  return;
}
```

**Step 4: Render `DiffView` when `viewMode === 'diff'`**

After the existing `if (viewMode === 'tasks') { ... }` render block (around line 212), add:
```tsx
if (viewMode === 'diff') {
  const session = sessions[selectedIndex];
  if (session) {
    return <DiffView session={session} onExit={() => setViewMode('list')} />;
  }
  // Session gone, fall through to list
}
```

**Step 5: Add `[d]Diff` to the keyboard shortcuts bar**

In the shortcuts `<Box>` (around line 256), add after `[s]Tasks`:
```tsx
<Text dimColor>[d]Diff</Text>
```

**Step 6: Run typecheck and lint**

```bash
npm run typecheck && npm run lint
```
Expected: no errors.

If lint reports fixable issues:
```bash
npm run lint:fix
```

**Step 7: Run existing tests to verify nothing is broken**

```bash
npm run test
```
Expected: all tests pass (handler, file-store, focus, send-text suites).

**Step 8: Commit**

```bash
git add src/components/DiffView.tsx src/components/Dashboard.tsx
git commit -m "feat: add [d] git diff view to ccn dashboard"
```

---

## Manual Smoke Test

1. Open a terminal in a git repo with uncommitted changes
2. Run `ccn` (or `npm run dev` from the repo root)
3. Select a session whose `cwd` has git changes
4. Press `d` → diff view opens showing changed files in the file strip
5. Press `j`/`k` to navigate files — file strip highlights update, diff content changes
6. Press `w`/`s` (or `↑`/`↓`) to scroll large diffs
7. Press `e` — VS Code opens the file (if `code` is in PATH)
8. Press `d`, `q`, or `Esc` → returns to the session list

Edge cases to verify:
- Session with `cwd` pointing to a non-git directory → "No changes detected" message
- Session with no unstaged/staged changes → "No changes detected" message
- File with only staged changes (after `git add`) → still appears in list
